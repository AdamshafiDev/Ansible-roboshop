- name: configure  mongodb sever
  become: yes
  hosts: mongodb
  tasks:
   - name: copy mongodb repos
     ansible.builtin.copy:
      src: mongo.repo
      dest: /etc/yum.repos.d/mongo.repo

   - name: install mongodb
     ansible.builtin.dnf:
       name: mongodb-org 
       state: present


      
